# Homebrew Tap for Circuit

Install Circuit on Apple Silicon macOS:

```bash
brew install --cask CIrcui-try/tap/circuit
```

Circuit is currently ad-hoc signed and not notarized. macOS Gatekeeper may require approval the first time you launch the app.
